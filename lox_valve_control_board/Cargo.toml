[package]
name = "lox_valve_control_board"
version = "0.1.0"
edition = "2021"
authors = ["Louis Schell <louis.schell1@gmx.de>"]

[dependencies]
embedded_utils = { path = "../embedded_utils" }

cortex-m = { workspace = true, features = ["critical-section-single-core"] }
cortex-m-rt = { workspace = true }

embassy-stm32 = { workspace = true, features = [
    "stm32g4a1ke",
    "time-driver-any",
    "memory-x",
    "exti",
    "unstable-pac",
] }
embassy-time = { workspace = true, features = ["tick-hz-32_768"] }
embassy-executor = { workspace = true, features = ["arch-cortex-m", "executor-thread"] }
embassy-futures = { workspace = true }
embassy-sync = { workspace = true }

defmt = { workspace = true, optional = true }
defmt-rtt = { workspace = true, optional = true }

panic-probe = { workspace = true, optional = true }
panic-reset = { workspace = true }

epos4 = { path = "../devices/epos4" }
embedded-io-async = "0.6.1"
rmodbus = { version = "^0.10.2", default-features = false, features = ["defmt", "heapless"] }
heapless = "0.8.0"
static_cell = "2.1.0"
bitflags = "1.3.2"

[build-dependencies]
built = { version = "0.7.7", features = ["git2"] }

[features]
default = ["debug"]
debug = ["defmt", "dep:panic-probe"]
defmt = [
    "dep:defmt",
    "dep:defmt-rtt",
    "panic-probe/defmt",
    "panic-probe/defmt-error",
    "panic-probe/print-defmt",
    "embassy-executor/defmt",
    "embassy-sync/defmt",
    "embassy-futures/defmt",
    "embassy-time/defmt",
    "embassy-time/defmt-timestamp-uptime",
    "embassy-stm32/defmt",
    "epos4/defmt",
]

[[bin]]
name = "lox_valve_control_board"
test = false
doctest = false
bench = false
